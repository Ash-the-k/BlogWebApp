<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Blogify - <%= title || 'Your Modern Blog Platform' %></title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.7/dist/css/bootstrap.min.css" rel="stylesheet"/>
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css"/>
  <link rel="stylesheet" href="/styles/main.css">
  <style>
    /* Custom scrollbar */
    ::-webkit-scrollbar {
      width: 8px;
    }
    ::-webkit-scrollbar-track {
      background: rgba(255, 255, 255, 0.1);
    }
    ::-webkit-scrollbar-thumb {
      background: var(--primary-color);
      border-radius: 4px;
    }
    
    /* Animation */
    @keyframes float {
      0%, 100% { transform: translateY(0); }
      50% { transform: translateY(-10px); }
    }
    
    .floating {
      animation: float 6s ease-in-out infinite;
    }
  </style>
</head>
<body>
  <header class="glass-nav fixed-top py-3">
    <div class="container col-xl-10">
      <nav class="d-flex flex-wrap justify-content-between align-items-center">
        <a href="/" class="d-flex align-items-center me-md-auto text-decoration-none">
          <svg xmlns="http://www.w3.org/2000/svg" width="35" height="28" fill="currentColor" class="bi bi-journals me-2" viewBox="0 0 16 16">
            <path d="M5 0h8a2 2 0 0 1 2 2v10a2 2 0 0 1-2 2 2 2 0 0 1-2 2H3a2 2 0 0 1-2-2h1a1 1 0 0 0 1 1h8a1 1 0 0 0 1-1V4a1 1 0 0 0-1-1H3a1 1 0 0 0-1 1H1a2 2 0 0 1 2-2h8a2 2 0 0 1 2 2v9a1 1 0 0 0 1-1V2a1 1 0 0 0-1-1H5a1 1 0 0 0-1 1H3a2 2 0 0 1 2-2"/>
            <path d="M1 6v-.5a.5.5 0 0 1 1 0V6h.5a.5.5 0 0 1 0 1h-2a.5.5 0 0 1 0-1zm0 3v-.5a.5.5 0 0 1 1 0V9h.5a.5.5 0 0 1 0 1h-2a.5.5 0 0 1 0-1zm0 2.5v.5H.5a.5.5 0 0 0 0 1h2a.5.5 0 0 0 0-1H2v-.5a.5.5 0 0 0-1 0"/>
          </svg>
          <span class="fs-4 fw-bold">Blogify</span>
        </a>

        <button class="navbar-toggler d-md-none btn-glass" type="button" data-bs-toggle="collapse" data-bs-target="#mobileNav">
          <i class="fas fa-bars"></i>
        </button>

        <ul class="nav nav-pills d-none d-md-flex gap-2">
          <li class="nav-item">
            <a href="/" class="nav-link <%= currentPath === '/' ? 'btn-glass-primary' : 'btn-glass' %>">
              <i class="fas fa-pen-fancy me-1"></i> Blog
            </a>
          </li>
          <li class="nav-item">
            <a href="/read" class="nav-link <%= currentPath === '/read' ? 'btn-glass-primary' : 'btn-glass' %>">
              <i class="fas fa-book-open me-1"></i> Read
            </a>
          </li>
          <li class="nav-item">
            <a href="/about" class="nav-link <%= currentPath === '/about' ? 'btn-glass-primary' : 'btn-glass' %>">
              <i class="fas fa-info-circle me-1"></i> About
            </a>
          </li>
        </ul>
      </nav>
    </div>

    <div class="collapse d-md-none mt-3" id="mobileNav">
      <div class="container col-xl-10 col-xxl-8">
        <div class="d-flex flex-column gap-2">
          <a href="/" class="btn <%= currentPath === '/' ? 'btn-glass-primary' : 'btn-glass' %>">
            <i class="fas fa-pen-fancy me-2"></i> Blog
          </a>
          <a href="/read" class="btn <%= currentPath === '/read' ? 'btn-glass-primary' : 'btn-glass' %>">
            <i class="fas fa-book-open me-2"></i> Read
          </a>
          <a href="/about" class="btn <%= currentPath === '/about' ? 'btn-glass-primary' : 'btn-glass' %>">
            <i class="fas fa-info-circle me-2"></i> About
          </a>
        </div>
      </div>
    </div>
  </header>

  <!-- Main content will be injected here -->
  <main class="container col-xl-10 col-xxl-10 px-4 py-5 mt-5">